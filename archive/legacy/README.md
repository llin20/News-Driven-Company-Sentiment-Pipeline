# Legacy Archive

This folder stores superseded project artifacts that are preserved for traceability but are not part of the current graded runtime path.

Active execution and setup guidance is documented in the repository root `README.md` and active Snowflake scripts under `snowflake/`.
